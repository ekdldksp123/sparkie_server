/* 커뮤니티 DB 관련 작업 서비스 */
import mongoose, {Schema, Document, Model} from "mongoose";

const Post = require('../src/database/schema/CommunitySchema');

export const getPosts = (key:string) => {
    
}

export const writePost = () => {

}

export const addComment = () => {
    
}